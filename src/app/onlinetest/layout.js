export const metadata = {
  title: "Online Examination | SoulKnower",
  description: "Secure online examination portal for SoulKnower candidates.",
  robots: { index: false, follow: false },
};

export default function OnlineTestLayout({ children }) {
  return children;
}
